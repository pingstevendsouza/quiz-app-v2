import themes from './theme';

// Export the themes object which contains both light and dark themes
export default themes;

// Also export individual themes for convenience
export const { light: lightTheme, dark: darkTheme } = themes;
